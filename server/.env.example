PORT=5000
# Local dev (no Atlas IP whitelist needed):
MONGO_URI=mongodb://127.0.0.1:27017/loan-management
# Atlas: add your IP under Network Access in the Atlas UI, then use your cluster URI
JWT_SECRET=replace_with_a_long_random_secret
